<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="18dp">

        <TextView style="@style/Title" android:text="VİOP" />

        <TextView
            android:id="@+id/providerStatus"
            style="@style/Sub"
            android:text="TradingView VİOP kaynağı kontrol ediliyor..." />

        <TextView
            android:id="@+id/status"
            style="@style/Sub"
            android:layout_marginTop="6dp"
            android:text="BIST futures sözleşmeleri TradingView üzerinden deneysel olarak keşfedilir. Veri yoksa sahte fiyat üretilmez." />

        <Button
            android:id="@+id/refresh"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:layout_marginTop="12dp"
            android:background="@drawable/bg_card_purple"
            android:textColor="@color/white"
            android:text="TRADINGVIEW VİOP TESTİNİ BAŞLAT" />

        <Button
            android:id="@+id/addContract"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:layout_marginTop="10dp"
            android:background="@drawable/bg_card_blue"
            android:textColor="@color/white"
            android:text="+ MANUEL SÖZLEŞME EKLE" />
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingHorizontal="12dp" />

    <include layout="@layout/include_bottom_nav" />
</LinearLayout>
