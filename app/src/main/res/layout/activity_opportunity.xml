<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bg_screen_gradient">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="18dp"
        android:paddingEnd="18dp"
        android:paddingTop="16dp"
        android:paddingBottom="12dp">

        <TextView style="@style/Title" android:text="Fırsat Kontrol" />
        <TextView android:id="@+id/txtSummary" style="@style/Sub" android:text="LONG / SHORT sonuçları Nihai Sinyal puanına göre sıralanır." android:layout_marginTop="2dp" />

        <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content" android:orientation="horizontal" android:layout_marginTop="12dp">
            <Button android:id="@+id/btnFilterLong" android:layout_width="0dp" android:layout_weight="1" android:layout_height="48dp" android:layout_marginEnd="5dp" android:text="LONG" android:textStyle="bold" android:textColor="@color/green" />
            <Button android:id="@+id/btnFilterShort" android:layout_width="0dp" android:layout_weight="1" android:layout_height="48dp" android:layout_marginHorizontal="5dp" android:text="SHORT" android:textStyle="bold" android:textColor="@color/red" />
            <Button android:id="@+id/btnFilterHighPower" android:layout_width="0dp" android:layout_weight="1" android:layout_height="48dp" android:layout_marginStart="5dp" android:text="85+" android:textStyle="bold" android:textColor="@color/yellow" />
        </LinearLayout>

        <Button
            android:id="@+id/btnRealOpportunityScan"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:layout_marginTop="12dp"
            android:background="@drawable/bg_card_blue"
            android:text="FIRSAT TARAMASINI BAŞLAT"
            android:textColor="@color/white"
            android:textStyle="bold" />

        <Button
            android:id="@+id/btnSignalHistory"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:layout_marginTop="8dp"
            android:text="SİNYAL GEÇMİŞİ / FORWARD TEST"
            android:textStyle="bold" />
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingBottom="8dp" />

    <include layout="@layout/include_bottom_nav" />
</LinearLayout>
