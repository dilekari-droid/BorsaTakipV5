diff --git a/app/build.gradle.kts b/app/build.gradle.kts
index b19bca1..d34bac9 100644
--- a/app/build.gradle.kts
+++ b/app/build.gradle.kts
@@ -12,7 +12,7 @@ android {
         applicationId = "tr.borsatakip.v5"
         minSdk = 26
         targetSdk = 35
-        versionCode = 124
+        versionCode = 125
         versionName = "5.2.5"
         testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
     }
diff --git a/app/src/main/java/tr/borsatakip/v5/analysis/ViopUnderlyingScanner.kt b/app/src/main/java/tr/borsatakip/v5/analysis/ViopUnderlyingScanner.kt
index d1c4b37..f8459d2 100644
--- a/app/src/main/java/tr/borsatakip/v5/analysis/ViopUnderlyingScanner.kt
+++ b/app/src/main/java/tr/borsatakip/v5/analysis/ViopUnderlyingScanner.kt
@@ -6,31 +6,39 @@ import kotlinx.coroutines.awaitAll
 import kotlinx.coroutines.coroutineScope
 import kotlinx.coroutines.sync.Semaphore
 import kotlinx.coroutines.sync.withPermit
+import tr.borsatakip.v5.data.CandleReadinessPolicy
 import tr.borsatakip.v5.data.MarketDataProvider
 import tr.borsatakip.v5.data.ViopBuiltinContractCatalog
+import tr.borsatakip.v5.model.Candle
 import tr.borsatakip.v5.model.Opportunity
+import tr.borsatakip.v5.model.Stock
 import tr.borsatakip.v5.model.ViopContract
 
 /**
  * Production VİOP quote/history bulunmadığında kullanılan DAYANAK İZLEME taraması.
- * Bu sınıf VİOP fiyatı, hacmi veya açık pozisyon üretmez. Yalnız ilgili BIST dayanağının
- * mevcut teknik motor çıktısını en yakın yerel kontrat etiketiyle birlikte sunar.
+ * VİOP fiyatı, hacmi veya açık pozisyon üretmez. Yalnız BIST dayanak verisini teknik olarak tarar.
+ *
+ * B125: quote ile history birbirine bağlı değildir. Quote geldiyse history ayrıca 60m/daily olarak
+ * tamamlanır. History yine yetersizse aday kaybolmaz; YETERSİZ VERİ olarak görünür.
  */
 class ViopUnderlyingScanner(private val provider: MarketDataProvider) {
     data class Candidate(
         val contract: ViopContract,
-        val underlying: Opportunity
+        val underlying: Opportunity,
+        val historyMode: String
     )
 
     data class Result(
         val items: List<Candidate>,
         val attempted: Int,
         val resolved: Int,
-        val failed: Int
+        val failed: Int,
+        val technicalReady: Int,
+        val quoteOnly: Int
     )
 
-    suspend fun scan(): Result = coroutineScope {
-        val nearestContracts = ViopBuiltinContractCatalog.current()
+    suspend fun scan(nowMs: Long = System.currentTimeMillis()): Result = coroutineScope {
+        val nearestContracts = ViopBuiltinContractCatalog.current(nowMs)
             .filter { it.underlying.matches(Regex("[A-Z0-9_]{3,12}")) }
             .filterNot { it.underlying == "XU030" }
             .groupBy { it.underlying }
@@ -40,23 +48,129 @@ class ViopUnderlyingScanner(private val provider: MarketDataProvider) {
         val semaphore = Semaphore(CONCURRENCY)
         val rows = nearestContracts.map { contract ->
             async(Dispatchers.IO) {
-                semaphore.withPermit {
-                    val stock = runCatching { provider.fetchOne(contract.underlying) }.getOrNull()
-                    val opportunity = stock?.let { OpportunityEngine.score(it) }
-                    if (opportunity == null) null else Candidate(contract, opportunity)
-                }
+                semaphore.withPermit { resolve(contract, nowMs) }
             }
         }.awaitAll()
 
