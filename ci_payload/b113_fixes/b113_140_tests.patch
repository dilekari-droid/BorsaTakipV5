--- a/app/src/main/java/tr/borsatakip/v5/analysis/OpportunityEngine.kt
+++ b/app/src/main/java/tr/borsatakip/v5/analysis/OpportunityEngine.kt
@@ -32,16 +32,25 @@
     }
 
     fun score(stock: Stock, kapLabel: String = "Veri yok"): Opportunity? {
-        val c = stock.candles
-            .filter { x ->
-                x.timestamp > 0L &&
-                    listOf(x.open, x.high, x.low, x.close, x.volume).all { it.isFinite() } &&
-                    x.open > 0.0 && x.high > 0.0 && x.low > 0.0 && x.close > 0.0 &&
-                    x.high >= max(x.open, x.close) && x.low <= min(x.open, x.close) && x.volume >= 0.0
-            }
+        val rawCandles = stock.candles
+        val hasMalformedCandle = rawCandles.any { x ->
+            x.timestamp <= 0L ||
+                listOf(x.open, x.high, x.low, x.close, x.volume).any { !it.isFinite() } ||
+                x.open <= 0.0 || x.high <= 0.0 || x.low <= 0.0 || x.close <= 0.0 ||
+                x.high < max(x.open, x.close) || x.low > min(x.open, x.close) || x.volume < 0.0
+        }
+        // Invalid OHLCV must never be silently removed and converted into a seemingly usable signal.
+        if (hasMalformedCandle) return null
+
+        val c = rawCandles
             .distinctBy { it.timestamp }
             .sortedBy { it.timestamp }
-        if (c.size < 220) return insufficientDataOpportunity(stock, c)
+        if (c.size < 220) {
+            // A structured provider snapshot may still be shown explicitly as INSUFFICIENT_DATA.
+            // Bare/incomplete candle-only inputs are rejected because their quote/session identity is unverified.
+            val hasStructuredQuote = stock.quotePrice?.let { it.isFinite() && it > 0.0 } == true && stock.lastBarTime > 0L
+            return if (hasStructuredQuote) insufficientDataOpportunity(stock, c) else null
+        }
 
         val price = stock.quotePrice?.takeIf { it.isFinite() && it > 0.0 } ?: c.last().close
         val prev = stock.previousClose?.takeIf { it.isFinite() && it > 0.0 }
--- a/app/src/main/java/tr/borsatakip/v5/analysis/OpportunityDiscoveryPresentation.kt
+++ b/app/src/main/java/tr/borsatakip/v5/analysis/OpportunityDiscoveryPresentation.kt
@@ -98,12 +98,15 @@
             val classification = when (x.decisionState) {
                 tr.borsatakip.v5.model.DecisionState.INSUFFICIENT_DATA -> "YETERSİZ VERİ"
                 tr.borsatakip.v5.model.DecisionState.REJECTED -> "REDDEDİLDİ"
-                else -> when {
-                    technicalAvailable && blended >= 82 && x.finalSignalScore >= 80 && x.direction.equals("LONG", true) && x.riskScore <= 40 -> "GÜÇLÜ AL ADAYI"
-                    technicalAvailable && blended >= 70 && x.finalSignalScore >= 70 && x.direction.equals("LONG", true) && x.riskScore <= 55 -> "AL ADAYI"
-                    technicalAvailable && blended >= 82 && x.finalSignalScore >= 80 && x.direction.equals("SHORT", true) && x.riskScore <= 40 -> "GÜÇLÜ SAT ADAYI"
-                    technicalAvailable && blended >= 70 && x.finalSignalScore >= 70 && x.direction.equals("SHORT", true) && x.riskScore <= 55 -> "SAT ADAYI"
-                    else -> "İZLE"
+                else -> {
+                    val verified = x.decisionState == tr.borsatakip.v5.model.DecisionState.VERIFIED_OPPORTUNITY
+                    when {
+                        technicalAvailable && blended >= 82 && x.finalSignalScore >= 80 && x.direction.equals("LONG", true) && x.riskScore <= 40 -> if (verified) "GÜÇLÜ AL" else "GÜÇLÜ AL ADAYI"
+                        technicalAvailable && blended >= 70 && x.finalSignalScore >= 70 && x.direction.equals("LONG", true) && x.riskScore <= 55 -> if (verified) "AL" else "AL ADAYI"
+                        technicalAvailable && blended >= 82 && x.finalSignalScore >= 80 && x.direction.equals("SHORT", true) && x.riskScore <= 40 -> if (verified) "GÜÇLÜ SAT" else "GÜÇLÜ SAT ADAYI"
+                        technicalAvailable && blended >= 70 && x.finalSignalScore >= 70 && x.direction.equals("SHORT", true) && x.riskScore <= 55 -> if (verified) "SAT" else "SAT ADAYI"
+                        else -> "İZLE"
+                    }
                 }
             }
             val reason = when (x.decisionState) {
--- a/app/src/main/java/tr/borsatakip/v5/ui/OpportunityActivity.kt
+++ b/app/src/main/java/tr/borsatakip/v5/ui/OpportunityActivity.kt
@@ -233,8 +233,8 @@
             val p = OpportunityDiscoveryPresentation.from(x)
             when (selectedFilter) {
                 DiscoveryFilter.ALL -> true
-                DiscoveryFilter.STRONG_BUY -> p.classification == "GÜÇLÜ AL ADAYI" || p.classification == "AL ADAYI"
-                DiscoveryFilter.STRONG_SELL -> p.classification == "GÜÇLÜ SAT ADAYI" || p.classification == "SAT ADAYI"
+                DiscoveryFilter.STRONG_BUY -> p.classification in setOf("GÜÇLÜ AL", "AL", "GÜÇLÜ AL ADAYI", "AL ADAYI")
+                DiscoveryFilter.STRONG_SELL -> p.classification in setOf("GÜÇLÜ SAT", "SAT", "GÜÇLÜ SAT ADAYI", "SAT ADAYI")
                 DiscoveryFilter.WATCH -> p.classification == "İZLE"
                 DiscoveryFilter.INSUFFICIENT -> p.classification == "YETERSİZ VERİ"
                 DiscoveryFilter.REJECTED -> p.classification == "REDDEDİLDİ"
--- a/app/src/main/java/tr/borsatakip/v5/data/ForwardOutcomeMatcher.kt
+++ b/app/src/main/java/tr/borsatakip/v5/data/ForwardOutcomeMatcher.kt
@@ -16,7 +16,11 @@
 
     fun dueHorizons(signalGeneratedAt: Long, now: Long, alreadyPresent: Set<String> = emptySet()): Map<String, Long> =
         horizons.filter { (name, horizon) ->
-            name !in alreadyPresent && signalGeneratedAt > 0L && now >= tradingTargetTime(signalGeneratedAt, horizon)
+            name !in alreadyPresent &&
+                signalGeneratedAt > 0L &&
+                horizon > 0L &&
+                signalGeneratedAt <= Long.MAX_VALUE - horizon &&
+                now >= signalGeneratedAt + horizon
         }
 
     /** Advances a wall-clock horizon to the next BIST trading session when it lands outside 09:40-18:10. */
--- a/app/src/main/res/layout/activity_main.xml
+++ b/app/src/main/res/layout/activity_main.xml
@@ -76,7 +76,7 @@
                         android:paddingTop="5dp"
                         android:paddingEnd="10dp"
                         android:paddingBottom="5dp"
-                        android:text="V5.1.49"
+                        android:text="—"
                         android:textColor="@color/blue"
                         android:textSize="12sp"
                         android:textStyle="bold" />
